# Source-only utilities: run with the existing interpreter, without installation.
[project]
name = "usdaeco"
version = "0.4.0"
description = "Release-pinned usdAECO suite superproject"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
